#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../oriento/bin/oriento" "$@"
  ret=$?
else 
  node  "$basedir/../oriento/bin/oriento" "$@"
  ret=$?
fi
exit $ret
